import pkg from '../../package.json';

export const APP_VERSION = pkg.version || '1.0.3';
export const APP_VERSION_LABEL = `v${APP_VERSION} — Enterprise Desktop`;
export const APP_VERSION_FULL_LABEL = `v${APP_VERSION} — Enterprise Desktop Edition`;
